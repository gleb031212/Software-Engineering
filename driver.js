class Driver {
    
    constructor(username, email, password) {
        let balance = 0;
    }
    parkingRequest(destination, arriveTime, endTime) {
        // Returns the assigned parking space
    }
    makePayment(amount) {
        balance = balance + amount;
    }
    parkingRequest(space, arriveTime, endTime) {
        // 
    }
    notifyDeparture() {
        
    }
}