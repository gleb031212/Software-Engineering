//event listener for #notify that listens for a click event and takes user to /notify securely
document.getElementById('notify').addEventListener('click', function() {
    window.location.href = '/notify';
    }
);
